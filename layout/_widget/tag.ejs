<% if (site.tags.length){ %>
  <div class="widget-wrap">
    <h6 class="widget-title"><%= __('tags') %></h6>
    <div class="widget">
      <%- list_tags({show_count: theme.show_count}) %>
    </div>
  </div>
<% } %>
